# Precog Assignment

## Language Representation Tasks

### File Structure

- `datasets/` — Contains the datasets used for the tasks.
- `models/` — Stores FastText vector files.
- `notebooks/` — Includes Jupyter notebooks used to perform the tasks. Each notebook is exported from Colab or Kaggle.